var forever = require('forever-monitor');

var start = function (userConfig, programsConfig) {
    // start the services according to the user settings using forever-monitor
};

module.exports = {
    start: start
};